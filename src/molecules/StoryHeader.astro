---
import Link from "../atoms/Link.astro";
import StoryImage from "../molecules/StoryImage.astro";
import Byline from "../molecules/Byline.astro";
---

<header class="flow article-top">

    <div class="flex-row">
      <ul role="list" class="info">

        <li>01. mars 2023</li>
        <li class="divider">|</li>
        <li>Lesetid: 2 min</li>
        <li class="divider">|</li>
        <li>Abonnent</li>
        <li class="divider">|</li>
        <Link href="/" text="Les mer om Finans " classes="tag" />
      </ul>
    </div>

    <h1>
      Ny konkursbølge under oppseiling: – Nå blir det verre
    </h1>
    <p class="lead">
      Konkurstallene stiger mot nye høyder. Så langt i år er antall konkurser innen detaljhandel og hotell/restaurant blitt mer enn doblet sammenlignet med i fjor.
    </p>
  </div>
  <StoryImage />
  <div class="byline">
      <Byline />
  </div>
</header>

<style>
  header {
    align-items: flex-start;
    color: var(--color-text-typo, currentColor);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    max-width: var(--grid-width);
    margin: 0 auto;
  }

    /* Article info Category and Date */
  .flex-row{
    display: flex;
    order: var(--hero-info);
    justify-content: space-between;
    inline-size: min(
      var(--grid-width),
      100vw - calc(var(--site-padding) * 2)
    );
  }


  ul[role="list"] {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-family: var(--typography-ui-font-family);
    color: var(--color-neutral-100);
    column-gap: .3rem;
    font-size: .8rem;

    & .divider{
      color: var(--color-neutral-600) ;
    }

      @media only screen and (min-width: 768px) {
        column-gap: 0.6rem;
           font-size: 1rem;
      }
  }



  /* Title */

  h1 {
    order: var(--hero-text);
    font-family: var(--hero-font, "SmBold23", sans-serif);
    font-weight: 400;
    font-size: var(--tile-size);
    line-height: 1.3;
    inline-size: min(
      var(--grid-width),
      100vw - calc(var(--site-padding) * 2)
    );
  }

  /* Innress */
  p.lead {
    order: var(--lead-text);
    font-size: var(--lead-size) !important;
    font-weight: 400;
    line-height: 1.2;
        inline-size: min(
      var(--grid-width),
      100vw - calc(var(--site-padding) * 2)
    );
  }
  /* Byline */

  .byline{
    order: var(--hero-byline);
    display: flex;
    flex-direction: column;
    row-gap: var(--row-gap);
    justify-content: space-between;
    inline-size: min(
      var(--grid-width),
      100vw - calc(var(--site-padding) * 2)
    );
    padding-block-end: var(--row-gap);

    @media only screen and (min-width: 768px) {
        flex-direction: row;
    }
  }

</style>
