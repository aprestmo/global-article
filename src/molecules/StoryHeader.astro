---
import StoryImage from "../molecules/StoryImage.astro";
import StoryMeta from "../molecules/StoryMeta.astro";
---

<header class="flow article-top">
  <h1>Folkehelseviter og Sveits-utflyttet influenser arver milliarder</h1>
  <p class="lead">
    Konkurstallene stiger mot nye høyder. Så langt i år er antall konkurser
    innen detaljhandel og hotell/restaurant blitt mer enn doblet sammenlignet
    med i fjor.
  </p>
  <StoryImage src="../img/topimage.jpg" title="This is a title" />
  <StoryMeta />
</header>

<style>
  header {
    /* background: linear-gradient(
      180deg,
      var(--color-links-default) 0%,
      var(--color-links-default) 42%,
      rgba(8, 34, 137, 0) 42%,
      rgba(0, 212, 255, 0) 100%
    ); */
    align-items: flex-start;
    color: var(--color-text-typo, currentColor);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    max-width: 100vw;
    width: 100%;
    margin-inline: auto;
    padding-bottom: var(--row-gap);
    margin-bottom: var(--row-gap);
  }

  /* Title */

  h1 {
    order: var(--hero-text);
    font-family: var(--hero-font, "SmBold23", sans-serif);
    font-weight: 700;
    font-size: var(--tile-size);
    margin-top: var(--row-gap);
    line-height: 1.3;
    inline-size: min(var(--grid-width), 100vw - calc(var(--site-padding) * 2));
  }

  /* Innress */
  p.lead {
    order: var(--lead-text);
    font-size: var(--lead-size) !important;
    font-weight: 400;
    line-height: 1.2;
    inline-size: min(var(--grid-width), 100vw - calc(var(--site-padding) * 2));
  }
</style>
