<figure>
  <picture>
    <img
      src="https://images.dn.no/image/ZXlucWphd1cyMG9ocVphSEdnTlhNaEZqenpZalNOdnY0U2VKL0RFamdIbz0=/nhst/binary/5d708bafc2b055cbe5f488bc7e995af4?image_version=1080"
      alt=""
    />
  </picture>
  <figcaption>
    Analytiker Brit Elise Melhus i Dun & Bradstreet blir ikke overrasket dersom
    antall konkurser i år vil havne på samme nivå som i rekordårene 2018 og
    2019. Da gikk rundt 5000 selskaper konkurs hvert år. (Foto: Javad Parsa)
  </figcaption>
</figure>

<style>
  figure {
    order: var(--hero-figure);

    margin: 0 auto;
    inline-size: min(
      var(--figure-width),
      100vw - calc(var(--site-padding) * 0)
    );

    @media only screen and (min-width: 768px) {
      inline-size: min(
        var(--figure-width),
        100vw - calc(var(--site-padding) * 2)
      );
    }
  }

  picture {
    display: flex;
    justify-content: center;
  }

  img {
    inline-size: var(--figure-width);
  }

  figcaption {
    color: var(--color-typography-secondary);
    font-family: var(--typography-ui-font-family), sans-serif;
    inline-size: min(var(--grid-width), 100vw - calc(var(--site-padding) * 2));
    font-size: 1rem;
    line-height: 1.5;
    margin-inline: auto;
    padding-block-start: 0.6rem;
  }
</style>
