<div class="grid">
  <slot />
</div>

<style>
  .grid {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    inline-size: min(var(--page-width), 100vw - calc(var(--site-padding) * 2));
    margin-inline: auto;
    padding-block: var(--row-gap);
    padding-inline: 0;
    column-gap: var(--row-gap);
    row-gap: var(--row-gap);
  }

  .grid :global(.teaser:nth-child(1)),
  .grid :global(.teaser:nth-child(2)) {
    grid-column: span 12;
    @media only screen and (min-width: 768px) {
      grid-column: span 6;
    }
  }

  .grid :global(.teaser:nth-child(7)) {
    grid-column: span 12;
    @media only screen and (min-width: 768px) {
      grid-column: span 4;
    }
  }

  .grid :global(.teaser:nth-child(8)) {
    grid-column: span 12;
    @media only screen and (min-width: 768px) {
      grid-column: span 4;
    }
  }

  .grid :global(.teaser:nth-child(9)) {
    grid-column: span 12;
    @media only screen and (min-width: 768px) {
      grid-column: span 4;
    }
  }
</style>
