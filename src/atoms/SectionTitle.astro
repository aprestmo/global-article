<div class="flow">
  <slot />
</div>

<style>
  div {
    margin: var(--row-gap);
    padding: 0.5rem 0;
    inline-size: min(var(--page-width), 100vw - calc(var(--site-padding) * 2));
    margin-inline: auto;
    border-bottom: 1px solid var(--color-page-divider);
  }
</style>
