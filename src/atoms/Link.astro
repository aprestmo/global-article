---
export interface Props {
  text?: string;
  href: string;
  classes?: string;
  icon?: string;
}

const { text, href, classes, icon } = Astro.props as Props;
---

<a href={href} class:list={[classes]}>
  {icon && <img class="icon" src={icon} />}
  <span class="text">{text}</span><slot /></a
>

<style>
  a {
    color: var(--color-link-default);
    font-family: var(--font-family-utility), sans-serif;
    font-weight: bold;
    font-size: 1rem;
    text-underline-offset: 0.4rem;
    text-decoration: none !important;

    &:hover {
      color: var(--color-link-hover);
    }

    &:active {
      color: var(--color-link-active);
    }
  }

  .icon {
    margin-right: 10px;
    max-width: 20px;
  }

  .tag {
    display: flex;
    align-items: center;
    /* border: 1px solid currentColor; */
    border-radius: var(--radius);
    font-size: var(--typography-utility-s-font-size);
    font-family: var(--typography-utility-s-font-family);
    inline-size: max-content;
    letter-spacing: 0.04em;
    line-height: 1;
    padding-block: 9px 10px;
    padding-inline: 20px;
    transition: background-color 200ms ease-in-out;
    background-color: var(--color-brand-utility-50);
    color: var(--color-brand-utility-900) !important;
    white-space: nowrap;

    &:focus-visible,
    &:hover {
      background-color: var(--color-brand-utility-100) !important;
    }
  }

  .button {
    display: flex;
    align-items: center;
    white-space: nowrap;
    /* border: 1px solid currentColor; */
    background-color: var(--color-brand-utility-700);
    color: var(--color-brand-utility-100) !important;
    border-radius: var(--radius);
    font-size: var(--typography-utility-s-font-size);
    font-family: var(--typography-utility-s-font-family);
    inline-size: max-content;
    letter-spacing: 0.04em;
    line-height: 1;
    padding-block: 9px 10px;
    padding-inline: 20px;
    transition: background-color 200ms ease-in-out;

    &:focus-visible,
    &:hover {
      background-color: var(--color-brand-utility-500) !important;
    }
  }

  .button.icon a svg {
    stroke: yellow !important;
    fill: ;
  }
</style>
